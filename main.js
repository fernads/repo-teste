let msg = prompt('Caso/motivo de consulta: ');
let ticket = Math.random() * 30 + 1;
console.log(`Motivo: ${msg} | Ticket (na fila): ${ticket}`);
//testando o source control com o git integrado no vscode (fazer controle de versoes do codigo)